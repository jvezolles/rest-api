spring:
  datasource:
    url: jdbc:postgresql://localhost/restapis
    username: restapis
    driverClassName: org.postgresql.Driver
logging:
  level:
    root: INFO
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
    com:
      jvezolles:
        api: DEBUG
